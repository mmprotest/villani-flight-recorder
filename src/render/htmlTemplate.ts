import { FlightEvent, ParsedSession } from "../providers/types.js"; import { GitInfo } from "../git/gitInfo.js"; const esc=(s:unknown)=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]!));
export function htmlTemplate(session:ParsedSession, git:GitInfo|null){ const warnings=[...session.warnings,...session.events.flatMap(e=>e.warnings??[])]; const events=session.events; return `<!doctype html><html><head><meta charset="utf-8"><title>Villani Flight Recorder</title><style>body{margin:0;font-family:system-ui;background:#0b1020;color:#e8eefc}header{padding:14px 20px;background:#111a33;border-bottom:1px solid #2d3b66}.grid{display:grid;grid-template-columns:30% 40% 30%;height:calc(100vh - 96px)}aside,main{overflow:auto;padding:16px;border-right:1px solid #263557}.event{display:block;width:100%;text-align:left;margin:6px 0;padding:10px;border:1px solid #33466f;background:#121b35;color:#eef;border-radius:8px}.event:hover{background:#192747}pre{white-space:pre-wrap;word-break:break-word;background:#050814;padding:12px;border-radius:8px}code{color:#9ee493}.meta{display:flex;gap:14px;flex-wrap:wrap;font-size:13px}.warn{color:#ffd166}</style></head><body><header><h2>Villani Flight Recorder</h2><div class="meta"><span>Provider: ${esc(session.provider)}</span><span>Repo: ${esc(git?.root??session.cwd??"")}</span><span>Branch: ${esc(git?.branch)}</span><span>Session: ${esc(session.sessionId)}</span><span>Model: ${esc(session.model)}</span><span>Started: ${esc(session.startedAt)}</span><span>Ended: ${esc(session.endedAt)}</span><span>Event count: ${events.length}</span><span>Warnings: ${warnings.length}</span><span>Git: ${git?git.dirty?"dirty":"clean":"unknown"}</span></div></header><div class="grid"><aside><h3>Timeline</h3>${events.map((e,i)=>`<button class="event" onclick="show(${i})"><b>${esc(e.title)}</b><br><small>${esc(e.type)} ${esc(e.timestamp)}</small></button>`).join("")}</aside><main><h3 id="t"></h3><div id="d"></div></main><aside><h3>Git / Warnings</h3><p class="warn">${warnings.map(esc).join("<br>")}</p><h4>Status</h4><pre>${esc(git?.status)}</pre><h4>Diff stat</h4><pre>${esc(git?.diffStat)}</pre><h4>Recent commits</h4><pre>${esc(git?.recentCommits)}</pre><h4>Diff</h4><pre>${esc(git?.diff)}</pre></aside></div><script>const events=${JSON.stringify(events).replace(/</g,"\\u003c")};function e(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function show(i){const ev=events[i];document.getElementById('t').textContent=ev.title;document.getElementById('d').innerHTML='<p><code>'+e(ev.type)+'</code></p><pre>'+e(ev.summary||ev.command||ev.stdout||ev.stderr||ev.diff||'')+'</pre><details><summary>Raw JSON</summary><pre>'+e(JSON.stringify(ev.raw,null,2))+'</pre></details>'}show(0)</script></body></html>`; }
