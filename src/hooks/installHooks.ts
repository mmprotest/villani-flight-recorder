import fs from "node:fs/promises"; import path from "node:path"; import { HOOK_DIR } from "../config.js";
export async function appendHook(provider:string, payload:unknown, cwd=process.cwd()){ const sid=(payload&&typeof payload==="object"&&("session_id" in payload)?String((payload as any).session_id):new Date().toISOString().slice(0,10)); const dir=path.join(cwd,HOOK_DIR); await fs.mkdir(dir,{recursive:true}); await fs.appendFile(path.join(dir,`${provider}-${sid}.jsonl`),JSON.stringify({provider,receivedAt:new Date().toISOString(),payload})+"\n"); }
async function backup(file:string){ try{ await fs.copyFile(file,`${file}.bak-${Date.now()}`);}catch{} }
export async function installHooks(cwd=process.cwd()){ const dir=path.join(cwd,".villani-flight-recorder"); await fs.mkdir(dir,{recursive:true}); const snippet={claude:{hooks:[{command:"villani-flight-recorder hook claude"}]},codex:{hooks:[{command:"villani-flight-recorder hook codex"}]}}; const file=path.join(dir,"hook-snippets.json"); await backup(file); await fs.writeFile(file,JSON.stringify({...snippet,pi:"Native Pi hook installation is uncertain; pipe JSON payloads to `villani-flight-recorder hook pi` if your Pi config supports command hooks."},null,2)); return file; }
