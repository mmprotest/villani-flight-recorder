import { FlightEvent, FlightEventType, Provider } from "../providers/types.js";
export function event(id:string, provider:Provider, type:FlightEventType, title:string, raw?:unknown, extra:Partial<FlightEvent>={}):FlightEvent { return {id,provider,type,title,raw,...extra}; }
export function textOf(v:unknown):string|undefined { if(typeof v==="string") return v; if(Array.isArray(v)) return v.map(textOf).filter(Boolean).join("\n")||undefined; if(v&&typeof v==="object"){ const o=v as Record<string,unknown>; return textOf(o.text??o.content??o.message??o.output); } return undefined; }
export function obj(v:unknown):Record<string,unknown>{return v&&typeof v==="object"&&!Array.isArray(v)?v as Record<string,unknown>:{};}
export function titleFor(type:FlightEventType, e:Partial<FlightEvent>):string { if(type==="user_message") return "User prompt"; if(type==="assistant_message") return "Assistant response"; if(type==="bash_command") return `Ran ${e.command??"shell command"}`; if(type==="file_read") return `Read ${e.path??"file"}`; if(type==="file_write") return `Wrote ${e.path??"file"}`; if(type==="file_edit") return `Edited ${e.path??"file"}`; if(type==="tool_call") return `Tool call${e.summary?`: ${e.summary}`:""}`; if(type==="tool_result") return "Tool result"; if(type==="error") return "Error"; if(type==="git_commit") return `Commit ${e.summary??""}`.trim(); return type.replaceAll("_"," "); }
